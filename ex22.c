#include <stdio.h>

void main() {
    int a, b, c;
    float x;

    /* (a) */
    a = 250;
    b = 85;
    c = 25;
    x = a / (b - c);
    printf("(a): x = %f\n", x);

    /* (b) */
    a = 300;
    b = 70;
    c = 70;
    x = a / (b - c);
    printf("(b): x = %f\n", x);

}
